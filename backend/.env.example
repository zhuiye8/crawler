# ==================== 数据库配置 ====================
# PostgreSQL数据库连接字符串（使用asyncpg驱动）
# 格式：postgresql+asyncpg://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/medical_news

# ==================== Redis配置 ====================
# Redis缓存连接字符串（用于会话管理和缓存）
# 格式：redis://主机:端口/数据库编号
REDIS_URL=redis://localhost:6379/0

# ==================== MinIO/S3对象存储配置 ====================
# 对象存储服务地址（用于存储文章内容和附件）
S3_ENDPOINT=http://localhost:9000
# MinIO访问密钥
S3_ACCESS_KEY=minioadmin
# MinIO安全密钥
S3_SECRET_KEY=minioadmin
# 原始数据存储桶名称
S3_BUCKET_RAW=medical-news-raw
# 清洗后数据存储桶名称
S3_BUCKET_CLEAN=medical-news-clean
# 附件存储桶名称
S3_BUCKET_ATTACHMENTS=medical-news-attachments

# ==================== OpenAI配置 ====================
# OpenAI API密钥（用于RAG问答和智能摘要）
# 请替换为你的真实API密钥
OPENAI_API_KEY=sk-your-openai-api-key-here

# ==================== API服务配置 ====================
# API服务监听地址（0.0.0.0表示监听所有网络接口）
API_HOST=0.0.0.0
# API服务端口号
API_PORT=8000
# CORS跨域允许的来源列表（多个来源用逗号分隔）
# 注意：生产环境应该配置实际的前端域名
API_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ==================== 速率限制配置 ====================
# 聊天API每分钟请求限制（暂未启用）
RATE_LIMIT_CHAT=10
# 搜索API每分钟请求限制（暂未启用）
RATE_LIMIT_SEARCH=60
# 文章API每分钟请求限制（暂未启用）
RATE_LIMIT_ARTICLES=60

# ==================== 运行环境配置 ====================
# 运行环境标识（development/production）
ENVIRONMENT=development

# ==================== 数据清理配置 ====================
# 软删除文章物理删除前的保留天数
CLEANUP_DELETED_AFTER_DAYS=30
